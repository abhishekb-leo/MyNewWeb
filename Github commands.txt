 or: git remote set-url [--push] <name> <newurl> [<oldurl>]

git remote set-url --push MyDropDownMenuEnabledSite 
new URL
https://github.com/abhishekb-leo/MyNewWeb.git

Old URL
https://github.com/abhishekb-leo/WebUI.git

=======================================================

usage: git remote add [<options>] <name> <url>

    -f, --fetch           fetch the remote branches
    --tags                import all tags and associated objects when fetching
                          or do not fetch any tag at all (--no-tags)
    -t, --track <branch>  branch(es) to track
    -m, --master <branch>
                          master branch
    --mirror[=(push|fetch)]
                          set up remote as a mirror to push to or fetch from


git remote add -m master MyWebsite https://github.com/abhishekb-leo/MyNewWeb.git > Success